# Copy to .env and adjust if needed
NGINX_PORT=8080
ADMINER_PORT=8082

DB_HOST=mariadb
DB_NAME=zoemkinderopvang
DB_USER=zoem
DB_PASSWORD=zoem_secret
DB_ROOT_PASSWORD=root_secret

APP_URL=http://localhost:8080
APP_ENV=production
LOG_LEVEL=info